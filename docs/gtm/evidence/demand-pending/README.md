# Demand-Pending Evidence

Ausgabe von `scripts/gtm/demand_scan_prepare.py`.

- Status `pending_legal_gate`: Score ≥ 50 → CRM Pending, **kein** Auto-Outreach
- Demo-Dateien stammen von `--demo` und enthalten Beispiel-URLs (nicht produktiv)
- Echte Treffer: JSON-Liste an `--input` übergeben, Output hier oder in eigenem Ordner

Siehe `docs/gtm/CONVERSION_LOOP_V1.md` und SOP Kundensuche.
