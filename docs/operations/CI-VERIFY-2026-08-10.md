# CI-Verify 2026-08-10 (System-CEO, E3) + Worker-Nachprüfung t_64ff597e

**Prüfzeit:** 2026-08-10 ~01:50 (E3) · Nachprüfung 2026-08-10 ~21:30 (Kanban-Worker t_64ff597e, Run 179)
**Methode:** HTTP-Checks + Branding-Spot (curl) + Authed-WebUI-Extension-Nachweis

## Sidebar-Ziele (Hermes-WebUI-Buttons) — 5/5 OK
| Button | Ziel | Status |
|---|---|---|
| agentmemory | http://127.0.0.1:3113/health | 200 ✅ |
| lightRAG | http://127.0.0.1:9622/health | 200 ✅ |
| 9Router | http://127.0.0.1:20128/v1/models | 200 ✅ (Auth-Header akzeptiert) |
| paperclip | http://127.0.0.1:3100/api/health | 200 ✅ (Factory live, Skills serviert, /api/skills liefert Skill-Liste) |
| n8n | http://127.0.0.1:5678/healthz | 200 ✅ (Titel: „NeXify AI — Workflow Automation") |

**Sidebar-Quelle verifiziert (Neu — t_64ff597e):** Buttons kommen aus WebUI-Extension `nexifyai-apps` (Repo `apps/hermes/extensions/nexifyai-apps/`, Runtime `/workspace/nexifyai/webui-data/extensions/nexifyai-apps/`). Manifest: 5 Extensions `enabled:true` (app-memory → /agentmemory/, app-wissen → /lightrag/, app-router → ai-router.nexifyai.cloud, app-automation → 127.0.0.1:5678, app-paperclip → 127.0.0.1:3100). Login (POST /api/auth/login → 200) + authed Index-Response beweist: ALLE 5 Skripte + app-shared.css werden injiziert (`extensions/nexifyai-apps/assets/app-{memory,wissen,router,automation,paperclip}.js`). WebUI-Proxies `/agentmemory/` + `/lightrag/` + `/lightrag/webui/` existieren (302 → Login, auth-forward aktiv).

## Interne UIs — CI-Branding-Spot
| App | Erreichbarkeit | CI-Farben (#C8FF00/#0A0A0A) | Fonts |
|---|---|---|---|
| AgentMemory-Viewer (:3113) | 200 (200KB HTML) | **JA** — `html[data-theme="dark"] { --bg:#0A0A0A; --accent:#C8FF00; --border-heavy: rgba(200,255,0,.25) }` ✅ | `--font-display:'Outfit'`, `--font-body:'Manrope'` (dark) ✅ |
| 9Router-UI (:8904) | 200 (JS-App) | **JA** — CSS `--color-brand-500:#C8FF00; --color-primary:#C8FF00; --bg-page:#0a0a0a; --bg:#09090b` (98dac860a6ea577d.css) + outfit/manrope ✅ | outfit + manrope (2×2 CSS) ✅ |
| LightRAG-WebUI (:9621) | 200 (SPA-Hülle) | **JA** — CSS `--background:#0A0A0A; --primary:#C8FF00; --accent:#C8FF00; --ring:#C8FF00` (index-DocW-KSH.css, 6×C8FF00/6×0A0A0A) ✅ | n/v (System-Fonts, kein Outfit/Manrope) ⚠️ |
| 9Router-Cockpit :20128 | 200 (Titel „NeXify AI — AI Router") | — | — |
| Hermes-WebUI :8787 | 200 (Login), Extension-Tags injiziert | neXify-Branding (Login „NeXify — Anmelden") | — |

## Befund
- **Buttons + Dienste: vollständig funktional** (Paperclip + n8n reaktiviert 2026-08-09/10).
- **CI-Tokens:** AgentMemory-Viewer, 9Router-UI (8904+20128), LightRAG-WebUI tragen #0A0A0A/#C8FF00 in ihren (minifizierten) Build-CSS — kein Patch nötig (wie spezifiziert). Einzige Abweichung: LightRAG nutzt System-Fonts statt Outfit/Manrope (nicht kritisch, interne Tool-UI).
- **Worker-Infra-Hinweis:** Kanban-Worker-Spawns sterben wiederholt (Daemon loggt crashed; Tasks bleiben running bis Reclaim) — separates Infra-Thema, siehe ZK.

## Empfehlung
Sidebar-/CI-Status als „funktional + CI-konform (3/3 UIs)" abschließen. LightRAG-Font-Abweichung bei nächstem LightRAG-Build mit aufnehmen (kein separater Patch — minifiziert).
