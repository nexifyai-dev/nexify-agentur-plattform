# Outreach data (runtime)

- `queue/` — JSONL leads (`outreach_pending` + `send_allowed` + **`consent=true`**)
- `state/` — sent-YYYY-MM-DD.jsonl, errors, dry_run (gitignored)
- `unsub/` — unsubscribed emails (gitignored)

**§7 UWG:** No cold email without opt-in. See `docs/gtm/UWG-EMAIL-OPTIN-ONLY.md`.

See `docs/operations/LEAD-OUTREACH-AUTOMATION.md`.
