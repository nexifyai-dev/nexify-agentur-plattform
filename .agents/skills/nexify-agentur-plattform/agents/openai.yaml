interface:
  display_name: "Nexify Agentur Plattform"
  short_description: "Repo-specific patterns and workflows for nexify-agentur-plattform"
  default_prompt: "Use the nexify-agentur-plattform repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true