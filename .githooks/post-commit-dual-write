#!/usr/bin/env bash
# FILE: .githooks/post-commit-dual-write
# NIR: 31.07.2026 10:55
# UPDATED: 02.08.2026 09:05
# WHAT: Alias/Compat — leitet auf .githooks/post-commit weiter
# WHY: Historischer Name aus #98; Git führt nur `post-commit` aus
# DOCS-REF: docs/operations/BRAIN-DUAL-WRITE.md

set -euo pipefail
ROOT="$(git rev-parse --show-toplevel 2>/dev/null || true)"
[[ -z "$ROOT" ]] && exit 0
exec bash "$ROOT/.githooks/post-commit"
