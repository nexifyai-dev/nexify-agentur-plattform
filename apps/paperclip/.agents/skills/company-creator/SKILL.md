# company-creator (placeholder)

Dieses Verzeichnis wurde angelegt, um einen dangling symlink in `apps/paperclip/.claude/skills/` zu neutralisieren.

- Zweck: Git/CI Tools dürfen beim Traversieren keine „beyond symbolic link“/Missing-Target Fehler auslösen.
- Nächster Schritt: echte Skill-Implementierung, falls benötigt.
