name: Deploy to VPS

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io

jobs:
  # === BUILD & PUSH ===
  build:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push website
        uses: docker/build-push-action@v5
        with:
          context: .
          file: deploy/docker/Dockerfile.website
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ghcr.io/${{ github.repository_owner }}/nexify-website:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push hermes
        uses: docker/build-push-action@v5
        with:
          context: .
          file: deploy/docker/Dockerfile.hermes
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ghcr.io/${{ github.repository_owner }}/nexify-hermes:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push paperclip
        uses: docker/build-push-action@v5
        with:
          context: .
          file: deploy/docker/Dockerfile.paperclip
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ghcr.io/${{ github.repository_owner }}/nexify-paperclip:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # === DEPLOY TO VPS (nur bei main-merge) ===
  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            cd /opt/nexifyai-cloud
            git fetch origin main
            git checkout main
            git pull origin main
            docker compose pull
            docker compose down --timeout 30
            docker compose up -d --remove-orphans
            # Dynamic Config in bestehenden Traefik kopieren
            cp deploy/traefik/dynamic-monorepo.yml /docker/traefik-vsrs/dynamic/nexifyai-monorepo.yml
            sleep 15
            bash infra/scripts/health-check.sh
